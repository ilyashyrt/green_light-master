import 'package:flutter/material.dart';

class AccountInfoSecreen extends StatelessWidget {
  const AccountInfoSecreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Center(
            child: Text('Flutter arc'),
          ),
        ],
      ),
    );
  }
}
